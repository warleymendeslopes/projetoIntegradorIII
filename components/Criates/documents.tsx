import {    Modal,    ModalContent,    ModalBody,    ModalHeader,    useDisclosure,} from '@nextui-org/react'import React from "react";import {Input} from "@nextui-org/react";import {Select, SelectItem} from "@nextui-org/react";import { IoCloudUploadOutline } from "react-icons/io5";import { ImFolderUpload } from "react-icons/im";import {Button} from '@nextui-org/button';export default function CrateDocument(){    const { isOpen, onOpen, onOpenChange } = useDisclosure()    const Clients = [        {label: "Jussara", value: "Jussara",},        {label: "warley", value: "Warley",},        {label: "Antonio", value: "Antonio", },        {label: "Fabricia", value: "Fabricia", },        {label: "Maria", value: "Maria", },    ];    return(        <>            <Button onClick={onOpen} className="button-new-documet-desk hidden md:flex items-center bg-customBlue text-buttontext p-3 rounded-md absolute">                <IoCloudUploadOutline /> Novo Documento            </Button>            <Button onClick={onOpen} className="button-new-documet-mob flex md:hidden items-center bg-customBlue text-buttontext p-5 rounded-full">                <ImFolderUpload />            </Button>            <Modal                isOpen={isOpen}                placement={'center'}                onOpenChange={onOpenChange}                className={`light`}            >                <ModalContent>                    <ModalHeader>Cadastrar novo Documento</ModalHeader>                    <ModalBody>                        <div className="flex w-full flex-wrap md:flex-nowrap gap-4">                            <Input type="text" label="Nome do Aquivo"/>                        </div>                        <div className="flex w-full flex-wrap md:flex-nowrap gap-4">                            <Input type="text" label="Descricao"/>                        </div>                        <div className="flex w-full flex-wrap md:flex-nowrap gap-4">                            <Select                                label="Selecione o Cliente"                                className="max-w-xs"                            >                                {Clients.map((client) => (                                    <SelectItem key={client.value} value={client.value}>                                        {client.label}                                    </SelectItem>                                ))}                            </Select>                        </div>                        <div className="flex w-full flex-wrap md:flex-nowrap gap-4">                            <input type="file"/>                        </div>                        <div className="flex w-full flex-wrap md:flex-nowrap gap-4">                            <Button className="flex items-center bg-customBlue text-buttontext p-5 rounded-md">                                ENVIAR DADOS                            </Button>                        </div>                    </ModalBody>                </ModalContent>            </Modal>        </>    )}