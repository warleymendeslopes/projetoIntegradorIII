import React from 'react';import Link from 'next/link';import { usePathname } from 'next/navigation';import { SlHome } from 'react-icons/sl';import { BsInfoSquare, BsEnvelopeAt } from 'react-icons/bs';import { FaTshirt, FaRedhat } from 'react-icons/fa';import { LuUsers2 } from "react-icons/lu";import { GrDocumentStore } from "react-icons/gr";export default function Sidebar({ show, setter }: any) {    const pathname = usePathname();    const className = "menu-lateral bg-black w-[250px] transition-[margin-left] ease-in-out duration-500 fixed md:static top-0 bottom-0 left-0 z-40";    const appendClass = show ? " ml-0" : " ml-[-250px] md:ml-0";    const MenuItem = ({ icon, name, route }: any) => {        const colorClass = pathname === route ? "text-black" : "text-black/50 hover:text-black";        return (            <Link                href={route}                onClick={() => {                    setter((oldVal: any) => !oldVal);                }}                className={`flex gap-1 [&>*]:my-auto text-md pl-6 py-3 border-b-[1px] border-b-white/10 ${colorClass}`}            >                <div className="text-xl flex [&>*]:mx-auto w-[30px]">                    {icon}                </div>                <div>{name}</div>            </Link>        );    };    const ModalOverlay = () => (        <div            className="flex md:hidden fixed top-0 right-0 bottom-0 left-0 bg-black/50 z-30"            onClick={() => {                setter((oldVal: any) => !oldVal);            }}        />    );    return (        <>            <div className={`${className}${appendClass}`}>                <div className="p-2 flex text-center	">                    <Link href="/document" className={'w-full\n'}>                        {/*eslint-disable-next-line*/}                        <h1 className={'title-logo'}><span style={{color: 'blue'}}>Doc</span>Control</h1>                    </Link>                </div>                <div className="flex flex-col">                    <MenuItem                        name="Documentos"                        route="/document"                        icon={<GrDocumentStore />}                    />                    <MenuItem                        name="Usuarios"                        route="/users"                        icon={<LuUsers2 />}                    />                </div>            </div>            {show ? <ModalOverlay /> : null}        </>    );}