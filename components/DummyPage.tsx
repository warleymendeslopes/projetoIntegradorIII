import React from 'react'import Layout from "@/components/Layout/page";export default function DummyPage({ title, description, children }: {title: string, description?: string, children: any}) {    return (        <Layout            pageTitle={title}        >            <div className="flex flex-col" style={{position: 'relative'}}>                <h1 className="text-4xl">{title}</h1>                <span className={'text-3x1'}>{description}</span>                <div className="body-dash my-10">                    {children}                </div>            </div>        </Layout>    )}